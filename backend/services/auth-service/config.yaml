server:
  host: "0.0.0.0"
  port: 8001

database:
  pool_size: 10
  max_overflow: 20
  pool_timeout: 30

jwt:
  algorithm: "HS256"
  access_token_expire_minutes: 30
  refresh_token_expire_days: 7

logging:
  level: "info"
  format: "json"
