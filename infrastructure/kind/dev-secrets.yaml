# ⚠️  DEV-ONLY secrets — NEVER use in production
# These match the secretKeyRef names used by the K8s deployment manifests.
# This file serves as a static fallback; for local dev, prefer running:
#   make kind-secrets  (sources .env.kind for customisable values)
#
# ── auth-service + api-gateway ───────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: auth-secrets
  namespace: ecommerce
type: Opaque
stringData:
  database-url: "postgresql+asyncpg://auth_user:change_me_auth@auth-db:5432/auth_db"
  # ⚠️  MUST be ≥ 32 chars (enforced at startup). Replace with a real secret
  #    in production: python3 -c "import secrets; print(secrets.token_hex(32))"
  jwt-secret: "dev_only_secret_replace_in_production_use_32chars_min"
---
# ── auth Postgres StatefulSet ─────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: auth-db-secrets
  namespace: ecommerce
type: Opaque
stringData:
  username: "auth_user"
  password: "change_me_auth"
---
# ── product-service ───────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: product-secrets
  namespace: ecommerce
type: Opaque
stringData:
  database-url: "postgresql+asyncpg://product_user:change_me_product@product-db:5432/product_db"
---
# ── product Postgres StatefulSet ──────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: product-db-secrets
  namespace: ecommerce
type: Opaque
stringData:
  username: "product_user"
  password: "change_me_product"
---
# ── cart-service ──────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: cart-secrets
  namespace: ecommerce
type: Opaque
stringData:
  redis-url: "redis://redis:6379/0"
---
# ── order-service ─────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: order-secrets
  namespace: ecommerce
type: Opaque
stringData:
  database-url: "postgresql+asyncpg://order_user:change_me_order@order-db:5432/order_db"
---
# ── order Postgres StatefulSet ────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: order-db-secrets
  namespace: ecommerce
type: Opaque
stringData:
  username: "order_user"
  password: "change_me_order"
---
# ── payment-service ───────────────────────────────────────────
# ⚠️  Replace stripe-secret-key and stripe-webhook-secret with real
#     Stripe test keys from https://dashboard.stripe.com/test/apikeys
#     or set them in .env.kind and run `make kind-secrets`.
apiVersion: v1
kind: Secret
metadata:
  name: payment-secrets
  namespace: ecommerce
type: Opaque
stringData:
  database-url: "postgresql+asyncpg://order_user:change_me_order@order-db:5432/order_db"
  stripe-secret-key: "sk_test_fake_key"
  stripe-webhook-secret: "whsec_fake_secret"
