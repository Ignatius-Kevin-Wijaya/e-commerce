# k6 Auth-Service Load Test Scripts — thesis recovery experiments
# Updated for the thesis recovery plan:
#   - Mixed auth traffic: 70% GET /auth/me + 30% POST /auth/login
#   - Token-pool setup retained for the /auth/me slice
#   - Env-driven BASE_VUS / PEAK_VUS for calibration ladders (closed-loop)
#   - Higher k6 resources to avoid client-side A1 false failures
#
# Usage:
#   kubectl apply -f k6-auth-job.yaml                     # Creates ConfigMaps + Jobs
#   The experiment runner only applies the ConfigMaps, then clones the Job
#   templates locally to create per-run Jobs with calibrated env overrides.

---
# ── k6 Script: Auth Gradual Ramp ────────────────────────────────────────────
# Linear increase from BASE_VUS → PEAK_VUS over 5 minutes.
apiVersion: v1
kind: ConfigMap
metadata:
  name: k6-auth-script-gradual
  namespace: ecommerce
data:
  load-test.js: |
    import http from 'k6/http';
    import { check } from 'k6';
    import { SharedArray } from 'k6/data';

    // VU-based parameters — each VU is one concurrent user (mirrors
    // k6-shipping-job.yaml). Calibrated 2026-08-15 against fixed-replica
    // B1/B2: B2 (5 pods) saturates at ~55 req/s and its throughput knee is
    // 12 VUS; B1 (1 pod) is already pinned at ~12 req/s. PEAK_VUS=12 gives a
    // 4.8x throughput / 25x median B1-vs-B2 validity gate with zero
    // load-phase errors on B2. BASE_VUS=3 keeps a single pod comfortable so
    // autoscalers start from a quiet baseline.
    const BASE_VUS = parseInt(__ENV.BASE_VUS || '3', 10);
    const PEAK_VUS = parseInt(__ENV.PEAK_VUS || '12', 10);
    const NUM_USERS = parseInt(__ENV.NUM_TEST_USERS || '120', 10);
    const AUTH_ME_PERCENT = parseInt(__ENV.AUTH_ME_PERCENT || '70', 10);
    const AUTH_LOGIN_PERCENT = parseInt(__ENV.AUTH_LOGIN_PERCENT || '30', 10);
    const MIX_TOTAL = Math.max(AUTH_ME_PERCENT + AUTH_LOGIN_PERCENT, 1);
    const AUTH_ME_THRESHOLD = AUTH_ME_PERCENT / MIX_TOTAL;

    export const options = {
      scenarios: {
        gradual_ramp: {
          executor: 'ramping-vus',
          startVUs: BASE_VUS,
          gracefulRampDown: '30s',
          stages: [
            { duration: '2m', target: BASE_VUS },
            { duration: '5m', target: PEAK_VUS },
            { duration: '2m', target: PEAK_VUS },
            { duration: '3m', target: 0 },
          ],
        },
      },
      thresholds: {
        http_req_duration: ['p(95)<2000'],
        http_req_failed: ['rate<0.05'],
      },
    };

    const BASE_URL = __ENV.TARGET_URL || 'http://auth-service.ecommerce.svc.cluster.local:8001';
    const GATEWAY_SECRET = __ENV.INTERNAL_GATEWAY_SECRET || '';
    const JSON_HEADERS = {
      'Content-Type': 'application/json',
      'X-Internal-Gateway-Secret': GATEWAY_SECRET,
    };

    const testUsers = new SharedArray('users', function () {
      const users = [];
      for (let i = 0; i < NUM_USERS; i++) {
        users.push({
          email: `loadtest_user_${i}@example.com`,
          username: `loadtest_user_${i}`,
          password: 'LoadTest_P@ss_2026!',
        });
      }
      return users;
    });

    function randomUser() {
      return testUsers[Math.floor(Math.random() * testUsers.length)];
    }

    export function setup() {
      console.log(`Seeding and pre-authenticating ${NUM_USERS} users...`);
      const tokens = [];

      for (let i = 0; i < NUM_USERS; i++) {
        const user = testUsers[i];

        // Login first, and register only if that fails. Registering
        // unconditionally made every run emit exactly NUM_USERS failed
        // /auth/register calls, because the users already exist from the
        // first run onward. That put a constant ~120 failures into
        // http_req_failed BEFORE any load was applied, which is the entire
        // reported error rate on otherwise-clean auth runs (e.g. b2/gradual
        // rep1: 120 out of 17339 = 0.69%, with checks_failed = 0).
        let loginRes = http.post(
          `${BASE_URL}/auth/login`,
          JSON.stringify({ email: user.email, password: user.password }),
          { headers: JSON_HEADERS }
        );

        if (loginRes.status !== 200) {
          http.post(
            `${BASE_URL}/auth/register`,
            JSON.stringify({
              email: user.email,
              username: user.username,
              password: user.password,
              full_name: `Load Test User ${i}`,
            }),
            { headers: JSON_HEADERS }
          );

          loginRes = http.post(
            `${BASE_URL}/auth/login`,
            JSON.stringify({ email: user.email, password: user.password }),
            { headers: JSON_HEADERS }
          );
        }

        if (loginRes.status === 200) {
          try {
            const body = JSON.parse(loginRes.body);
            if (body.access_token) {
              tokens.push(body.access_token);
            }
          } catch (e) {
            console.warn(`Failed to parse login response for user ${i}`);
          }
        } else {
          console.warn(`Login failed for user ${i}: ${loginRes.status}`);
        }
      }

      if (tokens.length === 0) {
        throw new Error('No tokens obtained during setup');
      }

      console.log(`Pre-auth complete: ${tokens.length}/${NUM_USERS} tokens obtained`);
      return { tokens };
    }

    function runAuthMe(data) {
      const token = data.tokens[Math.floor(Math.random() * data.tokens.length)];
      const res = http.get(
        `${BASE_URL}/auth/me`,
        {
          headers: {
            'X-Internal-Gateway-Secret': GATEWAY_SECRET,
            Authorization: `Bearer ${token}`,
          },
        }
      );
      check(res, {
        'me status is 200': (r) => r.status === 200,
        'me returns user data': (r) => {
          try {
            return JSON.parse(r.body).email !== undefined;
          } catch (e) {
            return false;
          }
        },
        'me response time < 500ms': (r) => r.timings.duration < 500,
      });
    }

    function runLogin() {
      const user = randomUser();
      const res = http.post(
        `${BASE_URL}/auth/login`,
        JSON.stringify({ email: user.email, password: user.password }),
        { headers: JSON_HEADERS }
      );
      check(res, {
        'login status is 200': (r) => r.status === 200,
        'login returns access token': (r) => {
          try {
            return JSON.parse(r.body).access_token !== undefined;
          } catch (e) {
            return false;
          }
        },
        'login response time < 1000ms': (r) => r.timings.duration < 1000,
      });
    }

    export default function (data) {
      if (Math.random() < AUTH_ME_THRESHOLD) {
        runAuthMe(data);
        return;
      }
      runLogin();
    }

---
# ── k6 Script: Auth Sudden Spike ────────────────────────────────────────────
# BASE_VUS warm-up → instant jump to PEAK_VUS at t=2min.
apiVersion: v1
kind: ConfigMap
metadata:
  name: k6-auth-script-spike
  namespace: ecommerce
data:
  load-test.js: |
    import http from 'k6/http';
    import { check } from 'k6';
    import { SharedArray } from 'k6/data';

    // VU-based parameters — each VU is one concurrent user (mirrors
    // k6-shipping-job.yaml). Calibrated 2026-08-15 against fixed-replica
    // B1/B2: B2 (5 pods) saturates at ~55 req/s and its throughput knee is
    // 12 VUS; B1 (1 pod) is already pinned at ~12 req/s. PEAK_VUS=12 gives a
    // 4.8x throughput / 25x median B1-vs-B2 validity gate with zero
    // load-phase errors on B2. BASE_VUS=3 keeps a single pod comfortable so
    // autoscalers start from a quiet baseline.
    const BASE_VUS = parseInt(__ENV.BASE_VUS || '3', 10);
    const PEAK_VUS = parseInt(__ENV.PEAK_VUS || '12', 10);
    const NUM_USERS = parseInt(__ENV.NUM_TEST_USERS || '120', 10);
    const AUTH_ME_PERCENT = parseInt(__ENV.AUTH_ME_PERCENT || '70', 10);
    const AUTH_LOGIN_PERCENT = parseInt(__ENV.AUTH_LOGIN_PERCENT || '30', 10);
    const MIX_TOTAL = Math.max(AUTH_ME_PERCENT + AUTH_LOGIN_PERCENT, 1);
    const AUTH_ME_THRESHOLD = AUTH_ME_PERCENT / MIX_TOTAL;

    export const options = {
      scenarios: {
        sudden_spike: {
          executor: 'ramping-vus',
          startVUs: BASE_VUS,
          gracefulRampDown: '30s',
          stages: [
            { duration: '2m', target: BASE_VUS },
            { duration: '10s', target: PEAK_VUS },
            { duration: '4m50s', target: PEAK_VUS },
            { duration: '2m', target: PEAK_VUS },
            { duration: '3m', target: 0 },
          ],
        },
      },
      thresholds: {
        http_req_duration: ['p(95)<2000'],
        http_req_failed: ['rate<0.05'],
      },
    };

    const BASE_URL = __ENV.TARGET_URL || 'http://auth-service.ecommerce.svc.cluster.local:8001';
    const GATEWAY_SECRET = __ENV.INTERNAL_GATEWAY_SECRET || '';
    const JSON_HEADERS = {
      'Content-Type': 'application/json',
      'X-Internal-Gateway-Secret': GATEWAY_SECRET,
    };

    const testUsers = new SharedArray('users', function () {
      const users = [];
      for (let i = 0; i < NUM_USERS; i++) {
        users.push({
          email: `loadtest_user_${i}@example.com`,
          username: `loadtest_user_${i}`,
          password: 'LoadTest_P@ss_2026!',
        });
      }
      return users;
    });

    function randomUser() {
      return testUsers[Math.floor(Math.random() * testUsers.length)];
    }

    export function setup() {
      console.log(`Seeding and pre-authenticating ${NUM_USERS} users...`);
      const tokens = [];

      for (let i = 0; i < NUM_USERS; i++) {
        const user = testUsers[i];

        // Login first, and register only if that fails. Registering
        // unconditionally made every run emit exactly NUM_USERS failed
        // /auth/register calls, because the users already exist from the
        // first run onward. That put a constant ~120 failures into
        // http_req_failed BEFORE any load was applied, which is the entire
        // reported error rate on otherwise-clean auth runs (e.g. b2/gradual
        // rep1: 120 out of 17339 = 0.69%, with checks_failed = 0).
        let loginRes = http.post(
          `${BASE_URL}/auth/login`,
          JSON.stringify({ email: user.email, password: user.password }),
          { headers: JSON_HEADERS }
        );

        if (loginRes.status !== 200) {
          http.post(
            `${BASE_URL}/auth/register`,
            JSON.stringify({
              email: user.email,
              username: user.username,
              password: user.password,
              full_name: `Load Test User ${i}`,
            }),
            { headers: JSON_HEADERS }
          );

          loginRes = http.post(
            `${BASE_URL}/auth/login`,
            JSON.stringify({ email: user.email, password: user.password }),
            { headers: JSON_HEADERS }
          );
        }

        if (loginRes.status === 200) {
          try {
            const body = JSON.parse(loginRes.body);
            if (body.access_token) {
              tokens.push(body.access_token);
            }
          } catch (e) {
            console.warn(`Failed to parse login response for user ${i}`);
          }
        } else {
          console.warn(`Login failed for user ${i}: ${loginRes.status}`);
        }
      }

      if (tokens.length === 0) {
        throw new Error('No tokens obtained during setup');
      }

      console.log(`Pre-auth complete: ${tokens.length}/${NUM_USERS} tokens obtained`);
      return { tokens };
    }

    function runAuthMe(data) {
      const token = data.tokens[Math.floor(Math.random() * data.tokens.length)];
      const res = http.get(
        `${BASE_URL}/auth/me`,
        {
          headers: {
            'X-Internal-Gateway-Secret': GATEWAY_SECRET,
            Authorization: `Bearer ${token}`,
          },
        }
      );
      check(res, {
        'me status is 200': (r) => r.status === 200,
        'me returns user data': (r) => {
          try {
            return JSON.parse(r.body).email !== undefined;
          } catch (e) {
            return false;
          }
        },
        'me response time < 500ms': (r) => r.timings.duration < 500,
      });
    }

    function runLogin() {
      const user = randomUser();
      const res = http.post(
        `${BASE_URL}/auth/login`,
        JSON.stringify({ email: user.email, password: user.password }),
        { headers: JSON_HEADERS }
      );
      check(res, {
        'login status is 200': (r) => r.status === 200,
        'login returns access token': (r) => {
          try {
            return JSON.parse(r.body).access_token !== undefined;
          } catch (e) {
            return false;
          }
        },
        'login response time < 1000ms': (r) => r.timings.duration < 1000,
      });
    }

    export default function (data) {
      if (Math.random() < AUTH_ME_THRESHOLD) {
        runAuthMe(data);
        return;
      }
      runLogin();
    }

---
# ── k6 Script: Auth Oscillating ─────────────────────────────────────────────
# Alternating between BASE_VUS and PEAK_VUS every 90 seconds.
apiVersion: v1
kind: ConfigMap
metadata:
  name: k6-auth-script-oscillating
  namespace: ecommerce
data:
  load-test.js: |
    import http from 'k6/http';
    import { check } from 'k6';
    import { SharedArray } from 'k6/data';

    // VU-based parameters — each VU is one concurrent user (mirrors
    // k6-shipping-job.yaml). Calibrated 2026-08-15 against fixed-replica
    // B1/B2: B2 (5 pods) saturates at ~55 req/s and its throughput knee is
    // 12 VUS; B1 (1 pod) is already pinned at ~12 req/s. PEAK_VUS=12 gives a
    // 4.8x throughput / 25x median B1-vs-B2 validity gate with zero
    // load-phase errors on B2. BASE_VUS=3 keeps a single pod comfortable so
    // autoscalers start from a quiet baseline.
    const BASE_VUS = parseInt(__ENV.BASE_VUS || '3', 10);
    const PEAK_VUS = parseInt(__ENV.PEAK_VUS || '12', 10);
    const NUM_USERS = parseInt(__ENV.NUM_TEST_USERS || '120', 10);
    const AUTH_ME_PERCENT = parseInt(__ENV.AUTH_ME_PERCENT || '70', 10);
    const AUTH_LOGIN_PERCENT = parseInt(__ENV.AUTH_LOGIN_PERCENT || '30', 10);
    const MIX_TOTAL = Math.max(AUTH_ME_PERCENT + AUTH_LOGIN_PERCENT, 1);
    const AUTH_ME_THRESHOLD = AUTH_ME_PERCENT / MIX_TOTAL;

    export const options = {
      scenarios: {
        oscillating: {
          executor: 'ramping-vus',
          startVUs: BASE_VUS,
          gracefulRampDown: '30s',
          stages: [
            { duration: '2m', target: BASE_VUS },
            { duration: '10s', target: PEAK_VUS },
            { duration: '1m20s', target: PEAK_VUS },
            { duration: '10s', target: BASE_VUS },
            { duration: '1m20s', target: BASE_VUS },
            { duration: '10s', target: PEAK_VUS },
            { duration: '1m20s', target: PEAK_VUS },
            { duration: '10s', target: BASE_VUS },
            { duration: '1m20s', target: BASE_VUS },
            { duration: '10s', target: PEAK_VUS },
            { duration: '50s', target: PEAK_VUS },
            { duration: '3m', target: 0 },
          ],
        },
      },
      thresholds: {
        http_req_duration: ['p(95)<2000'],
        http_req_failed: ['rate<0.05'],
      },
    };

    const BASE_URL = __ENV.TARGET_URL || 'http://auth-service.ecommerce.svc.cluster.local:8001';
    const GATEWAY_SECRET = __ENV.INTERNAL_GATEWAY_SECRET || '';
    const JSON_HEADERS = {
      'Content-Type': 'application/json',
      'X-Internal-Gateway-Secret': GATEWAY_SECRET,
    };

    const testUsers = new SharedArray('users', function () {
      const users = [];
      for (let i = 0; i < NUM_USERS; i++) {
        users.push({
          email: `loadtest_user_${i}@example.com`,
          username: `loadtest_user_${i}`,
          password: 'LoadTest_P@ss_2026!',
        });
      }
      return users;
    });

    function randomUser() {
      return testUsers[Math.floor(Math.random() * testUsers.length)];
    }

    export function setup() {
      console.log(`Seeding and pre-authenticating ${NUM_USERS} users...`);
      const tokens = [];

      for (let i = 0; i < NUM_USERS; i++) {
        const user = testUsers[i];

        // Login first, and register only if that fails. Registering
        // unconditionally made every run emit exactly NUM_USERS failed
        // /auth/register calls, because the users already exist from the
        // first run onward. That put a constant ~120 failures into
        // http_req_failed BEFORE any load was applied, which is the entire
        // reported error rate on otherwise-clean auth runs (e.g. b2/gradual
        // rep1: 120 out of 17339 = 0.69%, with checks_failed = 0).
        let loginRes = http.post(
          `${BASE_URL}/auth/login`,
          JSON.stringify({ email: user.email, password: user.password }),
          { headers: JSON_HEADERS }
        );

        if (loginRes.status !== 200) {
          http.post(
            `${BASE_URL}/auth/register`,
            JSON.stringify({
              email: user.email,
              username: user.username,
              password: user.password,
              full_name: `Load Test User ${i}`,
            }),
            { headers: JSON_HEADERS }
          );

          loginRes = http.post(
            `${BASE_URL}/auth/login`,
            JSON.stringify({ email: user.email, password: user.password }),
            { headers: JSON_HEADERS }
          );
        }

        if (loginRes.status === 200) {
          try {
            const body = JSON.parse(loginRes.body);
            if (body.access_token) {
              tokens.push(body.access_token);
            }
          } catch (e) {
            console.warn(`Failed to parse login response for user ${i}`);
          }
        } else {
          console.warn(`Login failed for user ${i}: ${loginRes.status}`);
        }
      }

      if (tokens.length === 0) {
        throw new Error('No tokens obtained during setup');
      }

      console.log(`Pre-auth complete: ${tokens.length}/${NUM_USERS} tokens obtained`);
      return { tokens };
    }

    function runAuthMe(data) {
      const token = data.tokens[Math.floor(Math.random() * data.tokens.length)];
      const res = http.get(
        `${BASE_URL}/auth/me`,
        {
          headers: {
            'X-Internal-Gateway-Secret': GATEWAY_SECRET,
            Authorization: `Bearer ${token}`,
          },
        }
      );
      check(res, {
        'me status is 200': (r) => r.status === 200,
        'me returns user data': (r) => {
          try {
            return JSON.parse(r.body).email !== undefined;
          } catch (e) {
            return false;
          }
        },
        'me response time < 500ms': (r) => r.timings.duration < 500,
      });
    }

    function runLogin() {
      const user = randomUser();
      const res = http.post(
        `${BASE_URL}/auth/login`,
        JSON.stringify({ email: user.email, password: user.password }),
        { headers: JSON_HEADERS }
      );
      check(res, {
        'login status is 200': (r) => r.status === 200,
        'login returns access token': (r) => {
          try {
            return JSON.parse(r.body).access_token !== undefined;
          } catch (e) {
            return false;
          }
        },
        'login response time < 1000ms': (r) => r.timings.duration < 1000,
      });
    }

    export default function (data) {
      if (Math.random() < AUTH_ME_THRESHOLD) {
        runAuthMe(data);
        return;
      }
      runLogin();
    }

---
# ── k6 Auth Job Template: Gradual Ramp ──────────────────────────────────────
apiVersion: batch/v1
kind: Job
metadata:
  name: k6-auth-test-gradual
  namespace: ecommerce
spec:
  template:
    metadata:
      labels:
        app: k6
        target: auth-service
    spec:
      containers:
        - name: k6
          image: grafana/k6:latest
          command: ["k6", "run", "--out", "json=/results/results.json", "/scripts/load-test.js"]
          env:
            - name: TARGET_URL
              value: "http://auth-service.ecommerce.svc.cluster.local:8001"
            - name: BASE_VUS
              value: "3"
            - name: PEAK_VUS
              value: "12"
            - name: AUTH_ME_PERCENT
              value: "70"
            - name: AUTH_LOGIN_PERCENT
              value: "30"
            - name: NUM_TEST_USERS
              value: "120"
            - name: INTERNAL_GATEWAY_SECRET
              valueFrom:
                secretKeyRef:
                  name: gateway-secrets
                  key: internal-gateway-secret
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "1500m"
              memory: "1Gi"
          volumeMounts:
            - name: k6-script
              mountPath: /scripts
            - name: k6-results
              mountPath: /results
      volumes:
        - name: k6-script
          configMap:
            name: k6-auth-script-gradual
        - name: k6-results
          emptyDir: {}
      restartPolicy: Never
  backoffLimit: 0

---
# ── k6 Auth Job Template: Sudden Spike ──────────────────────────────────────
apiVersion: batch/v1
kind: Job
metadata:
  name: k6-auth-test-spike
  namespace: ecommerce
spec:
  template:
    metadata:
      labels:
        app: k6
        target: auth-service
    spec:
      containers:
        - name: k6
          image: grafana/k6:latest
          command: ["k6", "run", "--out", "json=/results/results.json", "/scripts/load-test.js"]
          env:
            - name: TARGET_URL
              value: "http://auth-service.ecommerce.svc.cluster.local:8001"
            - name: BASE_VUS
              value: "3"
            - name: PEAK_VUS
              value: "12"
            - name: AUTH_ME_PERCENT
              value: "70"
            - name: AUTH_LOGIN_PERCENT
              value: "30"
            - name: NUM_TEST_USERS
              value: "120"
            - name: INTERNAL_GATEWAY_SECRET
              valueFrom:
                secretKeyRef:
                  name: gateway-secrets
                  key: internal-gateway-secret
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "1500m"
              memory: "1Gi"
          volumeMounts:
            - name: k6-script
              mountPath: /scripts
            - name: k6-results
              mountPath: /results
      volumes:
        - name: k6-script
          configMap:
            name: k6-auth-script-spike
        - name: k6-results
          emptyDir: {}
      restartPolicy: Never
  backoffLimit: 0

---
# ── k6 Auth Job Template: Oscillating ───────────────────────────────────────
apiVersion: batch/v1
kind: Job
metadata:
  name: k6-auth-test-oscillating
  namespace: ecommerce
spec:
  template:
    metadata:
      labels:
        app: k6
        target: auth-service
    spec:
      containers:
        - name: k6
          image: grafana/k6:latest
          command: ["k6", "run", "--out", "json=/results/results.json", "/scripts/load-test.js"]
          env:
            - name: TARGET_URL
              value: "http://auth-service.ecommerce.svc.cluster.local:8001"
            - name: BASE_VUS
              value: "3"
            - name: PEAK_VUS
              value: "12"
            - name: AUTH_ME_PERCENT
              value: "70"
            - name: AUTH_LOGIN_PERCENT
              value: "30"
            - name: NUM_TEST_USERS
              value: "120"
            - name: INTERNAL_GATEWAY_SECRET
              valueFrom:
                secretKeyRef:
                  name: gateway-secrets
                  key: internal-gateway-secret
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "1500m"
              memory: "1Gi"
          volumeMounts:
            - name: k6-script
              mountPath: /scripts
            - name: k6-results
              mountPath: /results
      volumes:
        - name: k6-script
          configMap:
            name: k6-auth-script-oscillating
        - name: k6-results
          emptyDir: {}
      restartPolicy: Never
  backoffLimit: 0
